# ============================================
# Environment Configuration Template
# ============================================

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT Configuration
JWT_ACCESS_SECRET=your-super-secret-access-key-here-min-32-characters
JWT_REFRESH_SECRET=your-super-secret-refresh-key-here-min-32-characters
JWT_ACCESS_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Cookie Configuration
COOKIE_EXPIRE=7

# Email Configuration (Brevo)
EMAIL_FROM_ADDRESS=your-email@example.com
EMAIL_FROM_NAME=Your App Name
BREVO_API_KEY=your-brevo-api-key-here

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173

# ============================================
# Security Notes:
# ============================================
# 1. Never commit the actual .env file to version control
# 2. Use strong, random secrets for JWT (min 32 characters)
# 3. Keep your API keys secure
# 4. Use different secrets for development and production
# 5. Rotate secrets periodically in production
# ============================================
