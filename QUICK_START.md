# 🚀 دليل البدء السريع - نظام SM-2

## ⚡ البدء في 3 خطوات

### 1️⃣ تحديث Backend
```bash
cd backend
npm start
```

### 2️⃣ تحديث Frontend

في ملف `react/src/components/Ger.jsx`:

```javascript
// قبل:
import FlashcardView from './GermanLearningApp/Flashcard/FlashcardView';

// بعد:
import FlashcardView from './GermanLearningApp/Flashcard/FlashcardViewNew';
```

### 3️⃣ تشغيل Frontend
```bash
cd react
npm run dev
```

---

## 🎮 الاستخدام

### الأزرار الجديدة:
- ❌ **لم أعرفها** (0) → مراجعة بعد 10 دقائق
- 😅 **صعب** (1) → مراجعة بعد يوم
- 👍 **جيد** (2) → نمو متوسط
- ⭐ **ممتاز** (3) → نمو سريع

### اختصارات لوحة المفاتيح:
- `Space` أو `Enter` → قلب البطاقة
- `0` → لم أعرفها
- `1` → صعب
- `2` → جيد
- `3` → ممتاز
- `←` → السابق
- `→` → التالي

---

## 🔄 ترحيل البيانات القديمة

### الطريقة 1: إعادة التعيين (موصى به)
```bash
# من أي مكان
curl -X POST http://localhost:3000/api/sentences/reset
```

### الطريقة 2: Script الترحيل
```bash
cd backend
node migrate.js
```

---

## 📊 API الجديد

### مراجعة بطاقة:
```javascript
POST /api/sentences/:id/review
{
  "quality": 2  // 0=خطأ، 1=صعب، 2=جيد، 3=ممتاز
}
```

### الجمل المستحقة:
```javascript
GET /api/sentences/due
```

### الإحصائيات:
```javascript
GET /api/stats
```

---

## 🎯 الفرق الأساسي

| القديم | الجديد |
|--------|--------|
| زرّين (صح/خطأ) | 4 أزرار (0-3) |
| فواصل ثابتة | فواصل ديناميكية |
| بدون عامل سهولة | عامل سهولة (1.3-3.0) |
| مراجعة بعد يوم | مراجعة فورية (10 دقائق) |

---

## 📚 التوثيق الكامل

راجع `SM2_INTEGRATION_GUIDE.md` للتفاصيل الكاملة.

---

## ✅ Checklist

- [ ] Backend يعمل على port 3000
- [ ] استبدلت `FlashcardView` → `FlashcardViewNew`
- [ ] Frontend يعمل على port 5173
- [ ] أعدت تعيين البيانات أو رحّلتها
- [ ] اختبرت الأزرار الأربعة
- [ ] تحققت من الإحصائيات

---

## 🎉 جاهز للانطلاق!

افتح المتصفح على: `http://localhost:5173`

**Happy Learning! 🚀**
