# 🔐 دليل إضافة نظام المصادقة

## 📦 الخطوة 1: تثبيت المكتبات المطلوبة

### Backend:
```bash
cd backend
npm install bcryptjs jsonwebtoken cookie-parser
```

### Frontend:
```bash
cd react
npm install jwt-decode
```

---

## 📂 الملفات التي سيتم إنشاؤها

### Backend (5 ملفات):
- `models/User.js` - نموذج المستخدم
- `middleware/auth.js` - middleware المصادقة
- `controllers/authController.js` - وظائف المصادقة
- `routes/authRoutes.js` - مسارات المصادقة
- `server.js` - (تحديث)

### Frontend (4 ملفات):
- `context/AuthContext.jsx` - Context للمصادقة
- `components/Auth/LoginForm.jsx` - نموذج تسجيل الدخول
- `components/Auth/RegisterForm.jsx` - نموذج التسجيل
- `components/Auth/AuthPage.jsx` - صفحة المصادقة الرئيسية

---

## 🚀 بعد التثبيت، شغّل:

```bash
# Backend
cd backend
npm start

# Frontend (نافذة أخرى)
cd react
npm run dev
```

---

## ✅ ما تم إنجازه:

- [x] نموذج المستخدم مع تشفير كلمة المرور
- [x] تسجيل الدخول والخروج
- [x] التسجيل للمستخدمين الجدد
- [x] JWT Authentication
- [x] حماية المسارات (Protected Routes)
- [x] ربط الجمل بالمستخدم
- [x] واجهة تسجيل دخول جميلة

---

## 🔒 كيف يعمل النظام؟

1. المستخدم يسجل حساب جديد
2. Backend يشفّر كلمة المرور ويحفظها
3. عند تسجيل الدخول، Backend يتحقق من البيانات
4. إذا صحيحة، يرسل JWT Token
5. Frontend يحفظ التوكن في localStorage
6. كل طلب يحتوي على التوكن في Headers
7. Backend يتحقق من التوكن قبل كل عملية

---

## 🎯 الخطوات التالية:

1. ✅ قم بتشغيل `npm install` في Backend
2. ✅ قم بتشغيل `npm install` في Frontend
3. ✅ أضف `JWT_SECRET` في `.env`
4. ✅ شغّل Backend و Frontend
5. ✅ جرّب التسجيل وتسجيل الدخول!
