# ⚡ دليل البدء السريع - نظام المصادقة

## 🚀 3 خطوات فقط!

### 1️⃣ تثبيت المكتبات (نافذة واحدة):
```bash
# Backend
cd backend
npm install bcryptjs jsonwebtoken cookie-parser

# Frontend
cd ../react
npm install
```

### 2️⃣ تشغيل Backend (نافذة 1):
```bash
cd backend
npm start
```

### 3️⃣ تشغيل Frontend (نافذة 2):
```bash
cd react
npm run dev
```

---

## ✅ هذا كل شيء!

افتح: **http://localhost:5173**

---

## 🎯 التجربة:

1. سجّل حساب جديد
2. سجّل دخول
3. أضف جمل
4. كل شيء يعمل! ✨

---

## 📝 ملاحظات مهمة:

- ✅ Backend على port **3000**
- ✅ Frontend على port **5173**
- ✅ MongoDB يجب أن يكون متصل
- ✅ JWT_SECRET موجود في `.env`

---

## 🐛 مشكلة؟

### Backend لا يعمل:
```bash
# تحقق من:
1. MongoDB متصل؟
2. المكتبات مثبتة؟ (npm install)
3. .env موجود؟
```

### Frontend لا يعمل:
```bash
# تحقق من:
1. Backend يعمل؟
2. Port 5173 فاضي؟
3. المكتبات مثبتة؟
```

---

## 📚 الملفات المهمة:

```
backend/
  ├── models/User.js              → نموذج المستخدم
  ├── middleware/auth.js          → الحماية
  ├── controllers/authController.js → المنطق
  └── server.js                   → (محدّث)

react/src/
  ├── context/AuthContext.jsx     → الحالة العامة
  ├── components/Auth/AuthPage.jsx → صفحة الدخول
  ├── utils/api.js                → API Helper
  └── App.jsx                     → (محدّث)
```

---

## 🎉 استمتع!

الآن لديك نظام مصادقة احترافي! 🔐✨
